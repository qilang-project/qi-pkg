// qi-pkg —— 用奇语写的 qi 包管理客户端
//
// 命令行工具是 命令行/主程序.qi（编译出 qipkg）。这里是可以被别的 qi 程序
// 直接导入的库层：
//
//   导入 Pkg::{打包, 解包, SHA256十六进制};
//   变量 包体: 整数 = 打包("我的包目录");
//   打印行(SHA256十六进制(包体));
//
// 分层（每层都能脱开上一层单独测）：
//   哈希.qi     对字节切片算 SHA-256
//   归档.qi     tar 读写 + gzip 收尾 + 打包排除规则
//   清单.qi     qi.toml / qi.lock / 安装标记 的读写
//   注册中心.qi 五个 HTTP 端点 + percent-encode
//   安装.qi     下载 → 校验 sha256 → 落地 → 传递依赖

包 Pkg;

公开 导入 哈希;
公开 导入 归档;
公开 导入 清单;
公开 导入 注册中心;
公开 导入 安装;
